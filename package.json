{
  "name": "val-broadcast-tool",
  "version": "1.0.0",
  "private": true,
  "description": "VALORANT match lookup UI for graphics broadcast production, backed by the official Riot Games API.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
